import { SignupForm } from '@/features/auth/SignupForm';

export const metadata = { title: 'Create Account — Digital Footprint Analyzer' };

export default function SignupPage() {
  return <SignupForm />;
}
